# 🚀 YATRAAI — MASTER CONFIGURATION FILE
# 7. VOMYRA AI VOICE (Call Assistant)
VOMYRA_API_KEY=
VOMYRA_PHONE_NUMBER=

# Duplicate this to .env.local and fill in your real keys.

# ─────────────────────────────────────────────────────────────────────────────
# 🛡️ SUPABASE CORE (Auth + Database)
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://ogrltimjrrvchqzlddqt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_AABBCC112233
SUPABASE_SERVICE_ROLE_KEY=sb_secret_X_Y_Z_REQUIRED_FOR_PNR_WORKER

# Email OTP: use Supabase mailer (6-digit). Set BOTH lines for PKCE-safe email links + verify route:
# NEXT_PUBLIC_EMAIL_OTP_MODE=supabase
# EMAIL_OTP_MODE=supabase

# ─────────────────────────────────────────────────────────────────────────────
# 💳 RAZORPAY — Secure Payment Flows
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_REPLACE_ME
RAZORPAY_KEY_ID=rzp_test_REPLACE_ME
RAZORPAY_KEY_SECRET=REPLACE_WITH_SECRET_FROM_DASHBOARD
RAZORPAY_WEBHOOK_SECRET=REPLACE_WITH_SECRET_CREATED_IN_WEBHOOK_PAGE
# Used by /api/booking/confirm provider callback
PROVIDER_CONFIRM_SECRET=REPLACE_WITH_SHARED_PROVIDER_SECRET
SYS_DEBUG_SECRET=REPLACE_WITH_INTERNAL_DEBUG_SECRET

# ─────────────────────────────────────────────────────────────────────────────
# 🧠 AI & MULTILINGUAL (Gemini + Bhashini)
# ─────────────────────────────────────────────────────────────────────────────
GEMINI_API_KEY=YOUR_GEMINI_STUDIO_KEY
# Optional: default is gemini-2.5-flash (1.5 Flash is discontinued on the API). Example override:
# GEMINI_MODEL=gemini-2.0-flash

# Bhashini: Real-time 22 Indian Language Service
BHASHINI_API_KEY=YOUR_BHASHINI_API_KEY
BHASHINI_USER_ID=YOUR_USER_ID
BHASHINI_ORG_ID=YOUR_ORG_ID
# Voice STT mode (pick one strategy):
# • Default: try Chrome Web Speech first; on network/VPN blocks, fall back to server ASR (/api/voice/transcribe).
# • Chrome only — never use server fallback (fix VPN/firewall or accept failures):
# NEXT_PUBLIC_VOICE_CHROME_SPEECH_ONLY=true
# • Server only — skip Chrome Web Speech entirely (needs BHASHINI_API_KEY):
# NEXT_PUBLIC_VOICE_SERVER_STT=true

# ─────────────────────────────────────────────────────────────────────────────
# ✈️ EXTERNAL SEARCH CONNECTORS (Amadeus)
# ─────────────────────────────────────────────────────────────────────────────
AMADEUS_API_KEY=YOUR_AMADEUS_CLIENT_ID
AMADEUS_API_SECRET=YOUR_AMADEUS_SECRET

# ─────────────────────────────────────────────────────────────────────────────
# 📲 TELEGRAM — Bot API (copy these into .env.local — never commit .env.local)
# ─────────────────────────────────────────────────────────────────────────────
# Server-only: used by /api/telegram/send, webhook handlers, automation.
# From @BotFather → /newbot → API token (keep secret).
TELEGRAM_BOT_TOKEN=

# Optional public hint: bot username without "t.me/" (with or without leading @).
# Shown on Booking → Connect Telegram. Users must open this bot and press Start once.
NEXT_PUBLIC_TELEGRAM_BOT_NAME=YourTourPlanBot

# Webhook (production): point Telegram to https://YOUR_DOMAIN/api/telegram/webhook after deploy.

# ─────────────────────────────────────────────────────────────────────────────
# 🚂 RAILWAY AUTOMATION (Internal Background Sync)
# ─────────────────────────────────────────────────────────────────────────────
# Public URL where your frontend is hosted (for PNR worker callback)
# In dev: http://localhost:3000
FRONTEND_API_URL=http://localhost:3000/api/mcp/pnr
POLL_INTERVAL_MS=300000
